PostgreSQL 접속
`psql -U postgres -d meetflow_ai` 실행시
-->  command not found 에러 발생시 이전에 사용했던 PostgreSQL 17 경로를 다시 등록하면 됨.
`export PATH="$PATH:/c/Program Files/PostgreSQL/17/bin`

uv 활성화
`source .venv/Scripts/activate`

